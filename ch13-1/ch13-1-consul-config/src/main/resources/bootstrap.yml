spring:
  application:
    name: consul-config
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      config:
        format: yaml
        prefix: config
        data-key: default
server:
  port: 8083